[project]
name = "fieldwork-copilot"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "aiofiles>=23.0",
    "openai>=1.40",
    "numpy>=1.26",
]
